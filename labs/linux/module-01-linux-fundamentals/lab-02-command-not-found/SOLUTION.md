# Solution: Command Not Found

## Step-by-Step Fix

```bash
echo "alias ll='ls -la'" >> ~/.bashrc
echo "export APP_ENV=staging" >> ~/.bashrc
```

Run `/check.sh` or use the web Check button to verify and pass.
